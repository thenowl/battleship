import './styles.css';
import { initPageLoad } from './GameRenderer/initPageLoad';
import { RenderGameboard } from './Gameboard/Gameboard.renderer';
import { Ship } from './Ship/Ship';
import { RenderShip } from './Ship/Ship.renderer';

const body = document.body;

// const gameBoard = new RenderGameboard('gameBoard');
// body.appendChild(gameBoard.gameBoard);

// const submarine = new Ship(2, 'submarine');
// const ship = new RenderShip(submarine);
// body.appendChild(ship.ship);
